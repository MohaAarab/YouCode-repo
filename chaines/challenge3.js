const prompt=require('prompt-sync')()

const chaine1=prompt('enter a chaines of characters 1 : ')
const chaine2=prompt('enter a chaines of characters 2 : ')

console.log(`les deux chaines concatene : ${chaine1}${chaine2}`);
